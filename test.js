const tp = require('./index');

const opt = {
	hsl: !1,
	normalize: false,
	numbers: cp.NUMBERS_COMPLEX
};

tp(i => z => z.pow(1 + i, 1 + i), 5, [[-1, -1, 1, 1], , , 1, opt], null, './vid.mp4', i => i / 10, !1, !0);